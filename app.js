const q=document.getElementById("q"), results=document.getElementById("results"); let idx=[]; fetch("index.json").then(r=>r.json()).then(x=>idx=x); q.addEventListener("input",()=>{let s=q.value.trim().toLowerCase(); if(!s){results.innerHTML="";return} let a=idx.filter(x=>x.t.toLowerCase().includes(s)).slice(0,80); results.innerHTML='<div class="muted">'+a.length+' نتيجة ظاهرة</div>'+a.map(x=>`<div class="result" onclick="location.hash='p${x.p}';document.getElementById('frame').src='pages/${x.p}.html'">صفحة ${x.p}: ${x.t}</div>`).join("")}); document.getElementById("frame").src="pages/1.html";